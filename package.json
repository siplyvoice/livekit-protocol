{
  "name": "github.com/siplyvoice/livekit-protocol",
  "private": true,
  "version": "1.35.0",
  "scripts": {
    "changeset": "changeset",
    "ci:publish": "pnpm --filter @siplyvoice/livekit-protocol run build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.9",
    "@livekit/changesets-changelog-github": "^0.0.4"
  }
}
